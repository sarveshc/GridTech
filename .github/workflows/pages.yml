name: Deploy\n'on': {push: {branches: ['main']}}\njobs:{build:{runs-on:'ubuntu-latest',steps:[{uses:'actions/checkout@v4'},{uses:'actions/setup-node@v4',with:{node-version:20}},{run:'npm ci || npm i'},{run:'npm run build'},{uses:'actions/upload-pages-artifact@v3',with:{path:'./out'}}]},deploy:{needs:'build',runs-on:'ubuntu-latest',steps:[{id:'deployment',uses:'actions/deploy-pages@v4'}]}}